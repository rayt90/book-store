import React, { Component } from 'react';

export default class Separator extends Component {
  render() {
    return (
      <div style={{ height:"5rem" }}>
      </div>
    );
  }
}

{/*This component's purpose is to simply prevent other components being rendered underneath the navbar*/}
